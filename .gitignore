node_modules/
dist/
*.log

# Harness runtime + per-task worktrees must never be committed or pollute clean-tree checks
.harness/
.claude/worktrees/

# Stage-2 oversight board: live emitted manifest (regenerated by board-bootstrap.sh; .example is committed)
harness/board.config

# Target repo map: the real file holds operator-absolute clone paths (instance data); .example is committed
harness/repos.config

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Init state (instance-specific; created by .forge/scripts/init.sh)
.forge/.initialized
